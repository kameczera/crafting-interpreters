use super::interpreter::*;
use super::object::*;
use super::exception::*;

pub struct LoxCallable {
}

impl LoxCallable {
    fn call(interpreter: Interpreter, arguments: Vec<Object>) -> Result<Object, Exception> {

    }
}